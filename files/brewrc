# ============================================================================
# === To use Homebrew ===
HOMEBREW=`brew --prefix`

## Utils installed through Python & Homebrew
export PATH=$HOMEBREW/sbin:$PATH
